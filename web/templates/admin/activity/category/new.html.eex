<%= render "form.html", changeset: @changeset,
                        action: admin_activity_category_path(@conn, :create, @activity),
                        back: admin_activity_category_path(@conn, :index, @activity),
                        activity: @activity,
                        images: @images %>
