<table class="table">
  <thead>
    <tr>
      <th>Filename</th>
      <th>Caption</th>
      <th>Default Image?</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <%= for image <- @images do %>
    <tr>
      <td><%= link image.filename, to: image_path(@image_path, [@conn, :show, @ancestor, image]) %></td>
      <td><%= caption image %> </td>
      <td>
      <%= if image.error do %>
        There was an error processing this image.
      <% else %>
        <%= Grid.AdminView.default_switcher @ancestor.default_image_id, image.id,
        image_path(@image_path, [@conn, :set_default, @ancestor, image]) %>
      <% end %>
      </td>

      <td class="text-right">
        <div class="dropdown actions">
          <button class="btn btn-xs btn-default dropdown-toggle" type="button" data-toggle="dropdown">
            Actions <span class="caret"></span>
          </button>
          <ul class="dropdown-menu dropdown-menu-right">
            <li>
              <%= link "Edit", to: image_path(@image_path, [@conn, :edit, @ancestor, image]), class: "edit" %>
            </li>
            <li>
              <%= link "Delete", to: image_path(@image_path, [@conn, :delete, @ancestor, image]), method: :delete, data: [confirm: "Are you sure?"], class: "delete" %>
            </li>
          </ul>
        </div>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>
