<%= render "form.html",
  changeset: @changeset,
  action: image_path(@image_path, [@conn, :update] ++ @ancestors ++ [@changeset.model]),
  back: image_path(@ancestor_path, [@conn, :show] ++ @ancestors)
%>
