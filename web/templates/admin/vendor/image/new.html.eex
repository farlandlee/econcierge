<%= render "form.html", changeset: @changeset,
                        action: admin_vendor_image_path(@conn, :create, @vendor.id),
                        back: admin_vendor_image_path(@conn, :index, @vendor.id) %>
