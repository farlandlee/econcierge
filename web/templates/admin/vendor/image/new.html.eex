<h2>Add image for <%= @vendor.name %></h2>

<%= render "form.html", changeset: @changeset,
                        action: admin_vendor_image_path(@conn, :create, @vendor.id) %>

<%= link "Back", to: admin_vendor_image_path(@conn, :index, @vendor.id) %>
