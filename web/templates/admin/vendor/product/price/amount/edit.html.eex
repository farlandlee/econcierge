<%= render "form.html", changeset: @changeset,
                        action: admin_vendor_product_price_amount_path(@conn, :update, @vendor, @product, @price, @amount),
                        back: admin_vendor_product_price_amount_path(@conn, :index, @vendor, @product, @price) %>
