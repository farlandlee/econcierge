<%= render "form.html", changeset: @changeset,
                        action: admin_vendor_product_price_amount_path(@conn, :create, @vendor, @product, @price),
                        back: admin_vendor_product_price_amount_path(@conn, :index, @vendor, @product, @price) %>
