<%= render "form.html", changeset: @changeset,
                        action: admin_vendor_product_price_path(@conn, :update, @vendor, @product, @price),
                        back: admin_vendor_product_path(@conn, :show, @vendor, @product) %>
