<%= render "form.html",
    changeset: @changeset,
    action: admin_vendor_product_start_time_path(@conn, :update, @vendor, @product, @changeset.model),
    back: admin_vendor_product_path(@conn, :show, @vendor, @product),
    seasons: @seasons %>
