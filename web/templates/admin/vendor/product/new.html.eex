<%=
  render "form.html",
    changeset: @changeset,
    action: admin_vendor_product_path(@conn, :create, @vendor, activity_id: @activity.id),
    back: admin_vendor_path(@conn, :show, @vendor),
    experiences: @activity.experiences,
    amenities: @activity.amenities,
    locations: @locations,
    add_location: @add_location
%>
