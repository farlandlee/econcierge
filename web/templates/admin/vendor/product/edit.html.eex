<%=
  render "form.html",
    changeset: @changeset,
    action: admin_vendor_product_path(@conn, :update, @vendor, @changeset.model),
    back: admin_vendor_path(@conn, :show, @vendor),
    experiences: @experiences
%>
