<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Activity</th>
      <th>Description</th>
      <th>Published?</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for product <- @products do %>
    <tr>
      <td><%= link product.name, to: admin_vendor_product_path(@conn, :show, @vendor, product) %></td>
      <td><%= product.activity.name %></td>
      <td><%= product.description %></td>
      <td> <%= check_icon product.published %> </td>

      <td class="text-right">
        <div class="dropdown actions">
          <button class="btn btn-xs btn-default dropdown-toggle" type="button" data-toggle="dropdown">
            Actions <span class="caret"></span>
          </button>
          <ul class="dropdown-menu dropdown-menu-right">
            <li>
              <%= link "Edit", to: admin_vendor_product_path(@conn, :edit, @vendor, product), class: "edit" %>
            </li>
            <li>
              <%= link "Delete", to: admin_vendor_product_path(@conn, :delete, @vendor, product), method: :delete, data: [confirm: "Are you sure?"], class: "delete" %>
            </li>
          </ul>
        </div>
      </td>
    </tr>
<% end %>
  </tbody>
</table>
