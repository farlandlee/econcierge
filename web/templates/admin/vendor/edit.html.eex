<%= render "form.html", changeset: @changeset,
                        action: vendor_path(@conn, :update, @vendor),
                        activity_types: @activity_types %>

<%= link "Back", to: vendor_path(@conn, :index) %>
