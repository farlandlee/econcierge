<%= render "form.html", changeset: @changeset,
                        action: admin_vendor_path(@conn, :update, @vendor),
                        activities: @activities %>

<%= link "Back", to: admin_vendor_path(@conn, :index) %>
